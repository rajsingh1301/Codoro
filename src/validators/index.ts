/**
 * @file src/validators/index.ts
 * @description Zod request validation schemas.
 * Ensures strict typing and checks inputs for route handlers, form submissions, and API bodies:
 * - createStreamSchema: Validate stream title, category
 * - createCommunitySchema: Validate community names, descriptions
 * - sendMessageSchema: Validate chat content length
 */

export {};
