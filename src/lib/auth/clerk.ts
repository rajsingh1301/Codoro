/**
 * @file src/lib/auth/clerk.ts
 * @description Clerk authentication helper utilities and custom middleware mappings.
 * Validates user sessions, scopes, profiles, and syncs data to local DB.
 */

export const authHelpers = {
  // TODO: Add helper wrappers for @clerk/nextjs auth() and currentUser()
  // getUserId: () => string
  // syncUserWithDb: (clerkUser: any) => Promise<void>
};
