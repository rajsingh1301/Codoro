/**
 * @file src/components/ui/index.ts
 * @description Export point for atomic UI components (primitives).
 * Core components include buttons, inputs, dialogs, sliders, and badges.
 * Design target: Reusable, accessible (Radix/Aria), customizable elements.
 */

export {};
