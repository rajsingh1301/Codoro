/**
 * @file src/store/index.ts
 * @description Global client-side state management (e.g. Zustand store).
 * Manages player state (volume, theater mode), chat sidebar toggles, and user navigation context.
 */

export {};
